build:
  nodes:
    analysis:
      project_setup:
        override:
          - 'true'
      tests:
        override:
          - php-scrutinizer-run
          - command: composer test
            coverage:
              file: clover.xml
              format: clover

filter:
  excluded_paths:
    - 'tests/*'

checks:
  php: true
