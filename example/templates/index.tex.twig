\documentclass[a4paper]{article}

\title{ {% if page.metadata.title is defined %}{{ page.metadata.title }}{% else %}Untitled{% endif %} }

{% if page.metadata.date is defined %}
\date{
    {% if instanceof(page.metadata.date, 'DateTime') %}
        {{ page.metadata.date.format('j F Y') }}
    {% else %}
        {{ page.metadata.date }}
    {% endif %}
}
{% endif %}

{% if page.metadata.author is defined %}
\author{ {{ page.metadata.author }} }
{% endif %}

\begin{document}
    \maketitle

    {{ page.body|md2latex }}

\end{document}
